on:
  push:
    branches:
      - master

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - name: "testing githup actions"
        run: pwd

      - uses: actions/checkout@v3
      - name: "printing folder"
        run: ls
